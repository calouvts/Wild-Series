{% extends 'base.html.twig' %}

{% block title %}Edit Program{% endblock %}

{% block body %}
    <h1>Edit Program</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-dark">{{ button_label|default('form.save'|trans) }}</button>
    {{ form_end(form) }}


    <a href="{{ path('program_index') }}">{{ 'form.backtolist'|trans }}</a>


{% endblock %}
