{% extends 'base.html.twig' %}

{% block title %}Edit Episode{% endblock %}

{% block body %}
    <h1>Edit Episode</h1>

    {{ include('actor/_form.html.twig', {'button_label': 'actor.update'|trans}) }}

    <a href="{{ path('actor_index') }}">back to list</a>

    {{ include('actor/_delete_form.html.twig') }}
{% endblock %}
