{% extends 'base.html.twig' %}

{% block title %}Actor{% endblock %}

{% block body %}
    <h1>Liste des acteurs : </h1>
{% for actor in actors %}

<div>
    <h2>{{ loop.index }} / - Actor :     <a href="{{ path("actor_show", { id: actor.id }) }}">{{ actor.name }}</a> </h2>

    {% else %}
        Aucun acteur
    {% endfor %}



    <a href="{{ path('app_index') }}">
        Retour à l'accueil
    </a>
{% endblock %}

