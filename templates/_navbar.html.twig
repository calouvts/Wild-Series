
<div class="fixed-top">
    <div class>
        <nav class="navigation navbar navbar-expand-md navbar-dark spacenav">
            <button class="navbar-toggler ml-auto text-white" type="button" data-toggle="collapse"
                    data-target="#navbarSupportedContent"
                    aria-controls="navbarSupportedContent" aria-expanded="false"
                    aria-label="Toggle navigation">
                <span class=" navbar-toggler-icon "></span>
            </button>
            <a class=" navcompany navbar-brand img d-none d-lg-block">
                            Wild Series
            </a>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav ml-auto ">
                    <li class="nav-item"><a class="nav-link text-white h4" href="#">Accueil</a></li>
                    <li class="nav-item"><a class="nav-link text-white h4" href="{{ path('program_index') }}">Séries</a></li>
                    <li class="nav-item"><a class="nav-link text-white h4" href="{{ path('actor_index') }}">Distribution</a></li>
                    <li class="nav-item"><a class="nav-link text-white h4" href="/#">Favoris</a></li>
                   </ul>
                </ul>
            </div>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav ml-auto ">
                   <li class="nav-item"><a class="nav-link text-white" {% if app.user %}
                        {{ app.user.email }}
                        <a href="{{ path('app_logout') }}"> Se déconnecter</a>
                           <a href="{{ path('user_profile') }}"> Mon compte</a>

                    {% else %}
                        <a href="{{ path('app_login') }}"> Se connecter</a>
                    {% endif %}

                </ul>
            </div>
        </nav>
    </div>
</div>
